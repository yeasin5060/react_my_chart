/*import { createSlice } from '@reduxjs/toolkit'

export const googleSlice = createSlice({
  name: "googlealldata",
  initialState: {
    value: localStorage.getItem("googleuser") ? JSON.parse(localStorage.getItem("googleuser")) : null,
  },
  reducers: {
    googleuserdata: (state, action) => {
      state.value = action.payload
    },
  },
})

// Action creators are generated for each case reducer function
export const { googleuserdata } = googleSlice.actions

export default googleSlice.reducer*/